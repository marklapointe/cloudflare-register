#!/usr/bin/make -f

include /usr/share/dpkg/default.mk

export DH_VERBOSE = 1
export PYBUILD_NAME = cloudflare-register
export PYBUILD_SYSTEM = pep517
export PYBUILD_DISABLE_python3 = test

%:
	dh $@ --with python3 --buildsystem=pep517
